{% extends 'base.html.twig' %}

{% block content %}
	<section id="services" class="odd">
	    <div class="container">
	        <div class="row">
	            <div class="col-lg-12 text-center">
	                <h2 class="section-heading">Et maintenant ?</h2>
	                <h3 class="section-subheading text-muted">Maintenant il vous suffit de sélectionner les colonnes trello que vous souhaitez imprimer.</h3>
	            </div>
	        </div>
		    <form action="list" method="post">
	            {% for form in forms %}
			        <div class="row">
			            <div class="col-lg-12 text-left">
			            	{{ form_widget(form) }}
			            	{{ form_label(form) }}
		    	        </div>
		    	    </div>
	            {% endfor %}
			    <button type="submit" class="btn standard-button" name="upload">Imprimer</button>
			</form>
	        </div>
{% endblock %}